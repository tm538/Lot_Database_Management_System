<% provide(:title, 'Search') %>
<h1>Search</h1>

<div class="row">
  <div class="span12 offset">
  	
		<%= render 'searches/new' %>
		
		<%= render 'searches/results' %>
		
		<%= render 'searches/download' %>
		
	</div>
</div>
