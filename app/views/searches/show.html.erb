<% provide(:title, 'Search') %>
<h1>Search</h1>

<div class="row">
  <div class="span10 offset1">
  	
		<%= render 'searches/new' %>
		
		<%= render 'searches/results' %>
		
		<%= render 'searches/download' %>
		
	</div>
</div>
